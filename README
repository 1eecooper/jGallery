Simple Android Gallery

- displays dynamic image gallery from Picture folder and from URL list
- starts preloading images when launched
- shows 10 images per page, is able to scroll over all items within an image gallery
- uses both memory and disk caches
- supports optimized memory usage
